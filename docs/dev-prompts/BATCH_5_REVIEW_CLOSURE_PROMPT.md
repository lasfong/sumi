# Batch 5 final review closure — standalone DEV prompt

You are the DEV task for one bounded Batch 5 release-evidence closure. V3 is not approved and no release/tag/commit/push is authorized. Work only on B5-R01–B5-R05 in `docs/reviews/BATCH_5_REVIEW_2026-07-19.md`.

## Required reading

Read completely: `AGENTS.md`; all canonical V3 documents in its required order; `PLANS.md`; `docs/dev-prompts/BATCH_5_PRODUCT_HARDENING_V3_RC_PROMPT.md`; `docs/exec-plans/BATCH_5_PRODUCT_HARDENING_V3_RC.md`; `docs/reviews/BATCH_5_REVIEW_2026-07-19.md`; and the four V3 release/evidence documents created by Batch 5.

Preserve the shared dirty checkout. Do not create/switch a branch or worktree; stage, commit, push, merge, reset, clean, checkout or tag; mutate `backend/sumi.db`; transmit data externally; add a dependency; weaken acceptance; or start post-V3 work.

Before code changes, append closure scope, red evidence, affected modules, acceptance mapping, rollback and exact commands to the Batch 5 ExecPlan.

## Closure requirements

### B5-R01 — keyboard isolation

Record red focused/browser evidence that ArrowRight on the Trade tab advances both tab and replay, using the existing canonical timeline indices as corroboration. Implement one reusable global-shortcut eligibility policy covering default-prevented events and interactive/editable/modal targets. Add component propagation only as defense in depth.

Prove exact invariants for PracticeRail Arrow/Home/End, Space/Enter on buttons, all dialogs, form/contenteditable controls and drawing inspector fields. Replay/drawing state must remain byte/ID/index equal. Then prove Space/Arrow/Delete/Escape work exactly once from the intended non-interactive workspace.

### B5-R02 — full continuous future boundary

Extend every sustained checkpoint with authoritative session index/date, API candle count/max date, chart input count/max date, active indicator input/response max dates, marker max date and visible provider drawing dates. Reconcile retained canonical future drawings with visible provider filtering explicitly. Fail fast and retain partial/live evidence per surface. Add negative harness self-tests for each omitted surface.

### B5-R03 — fail-closed evidence bundle

Make baseline audit and canonical manifest validate the actual product result and all retained evidence. Require 272 baseline IDs unchanged and passing, additive closure IDs passing/blocking, zero blocking/runtime/provider/indicator failures except explicitly bounded expected rejection messages, migrations/recovery/restore passing, all cited files present with SHA-256, unchanged production DB, and exact expected HEAD/tag.

Add automated negative fixtures for failed additive check, duplicate/missing/changed baseline, nonzero blocking, missing artifact, recovery/restore/migration failure and DB/provenance mismatch. Every fixture must make the audit/manifest command nonzero.

### B5-R04 — accessibility evidence

Measure focus style before/after keyboard focus, its actual color contrast with the adjacent background, and retain a focused screenshot. At 200%, assert real bounding-box intersection, scroll/reflow geometry, keyboard reachability and activation of Replay, Trade and Journal. Do not use `isVisible()` alone. If meeting the recorded contract requires a nontrivial responsive redesign or conflicts with the supported minimum width, stop with evidence for Reviewer rather than changing the criterion.

### B5-R05 — complete sustained/performance scope

Timestamp all core action categories, including the short workflow before the tail loop. Mechanically require category coverage and meaningful activity in each five-minute window. Move the measured stress phase to a genuinely long visible history, retain its visible count/date, and include ten real analytics/replay route remounts plus reloads in matched memory/DOM/ownership/request measurements. Exercise all drawing tools plus edit/history/magnet and the complete trade/journal lifecycle within the recorded session.

The final canonical closure must run for at least 1,800 real elapsed seconds. Preserve all 272 current IDs exactly once and add unique blocking `batch5.closure.*` IDs for B5-R01–B5-R05. Compare against the canonical 272-ID baseline at `test-results/batch5-hardening/2026-07-19T01-05-23Z/product-uat/2026-07-19T01-05-26-343Z/results.json`.

## Final gates and handoff

Run focused red/green tests, audit/manifest negative self-tests, full backend/frontend/lint/build, `verify-v2.sh`, standalone UAT, `verify-product.sh`, and the complete `run-batch5-hardening.sh`. Review fresh 1440×1000, 1280×800, 200%, focused-control, long-history stress and restored-workspace screenshots.

Report exact counts, 272-ID comparison, additive IDs, 30-minute category/window coverage, all future-surface checkpoint fields, performance samples/budgets, negative manifest cases, recovery hashes, error arrays, DB hash, HEAD/tag and dirty-scope audit. Update all V3 evidence documents to point only to the final canonical closure bundle, while retaining the returned run as history. Do not self-approve or use release-ready/product-complete language. Stop at Reviewer gate.
