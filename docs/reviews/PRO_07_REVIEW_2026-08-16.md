# PRO-07 Independent Reviewer Record — 2026-08-16

## Verdict

`APPROVE` — PRO-07 (Ichimoku Contract) satisfies all acceptance criteria (PRO-IND-01 through PRO-IND-08, PRO-IND-10, and PRO-IND-11). Ichimoku Cloud is fully implemented as a price overlay with backend calculation authority, strict no-look-ahead displacement handling, parameter-exact column mapping (`ITS_${tenkan}`, `IKS_${kijun}`, `ISA_${tenkan}`, `ISB_${kijun}`, `ICS_${kijun}`), semantic multi-series rendering (`tenkan`, `kijun`, `spanA`, `spanB`, `chikou`), and browser-verified replay behavior across replay stepping, rewind, reload, and navigation. PRO-07 is closed. PRO-08 remains unauthorized.

## Implementation & Quality Audit

1. **Backend Calculation & Strategy Adapter Support**:
   - `IndicatorEngine._append_indicator_result` updated to cleanly preserve multi-DataFrame / tuple indicator results without overwriting data with disaligned future rows.
   - `IndicatorEngine.compute` produces verified column names: `ITS_${tenkan}`, `IKS_${kijun}`, `ISA_${tenkan}`, `ISB_${kijun}`, `ICS_${kijun}`.
   - `StrategyIndicatorAdapter._params_for` and `IndicatorConfig` support parameters: `tenkan`, `kijun`, `senkou`.
   - Comprehensive unit, edge, non-default parameter, and end-to-end parity tests pass (`test_indicators.py`, `test_indicator_parity_e2e.py` — 27/27 passed).
2. **Frontend Typed Catalog & Domain**:
   - `ichimoku` released in `SUPPORTED_INDICATORS` (`indicatorDomain.ts`).
   - Default styles (`tenkan`: `#26A69A`, `kijun`: `#EF5350`, `spanA`: `#00E5FF`, `spanB`: `#FF8A00`, `chikou`: `#E040FB`), parameters (`tenkan`: 9, `kijun`: 26, `senkou`: 52), and `price` overlay placement are defined cleanly.
   - `IndicatorManager.tsx` empty-state copy is updated.
3. **Renderer Registry & Exact Contracts**:
   - `IndicatorRenderRegistry.ts` maps exact parameter-derived columns: `ITS_${tenkan}`, `IKS_${kijun}`, `ISA_${tenkan}`, `ISB_${kijun}`, `ICS_${kijun}`.
   - All-or-nothing multi-series rendering is enforced across all five Ichimoku series.
   - Rejection of partial payloads or mismatched parameters is verified in vitest unit tests.
4. **No-Look-Ahead Displacement Invariant (PRO-IND-10)**:
   - Calculation timestamps remain strictly bounded by `current_index`. No future candle information or look-ahead signals can leak through displacement.
   - Display points map strictly to timestamp rows returned from the backend (bounded by `current_index`).
5. **Independent Tests & Gate Verification**:
   - Backend unit and parity tests pass (27/27).
   - Frontend unit tests pass (52/52 across 6 test files).
   - Fast technical gate (`verify-v2.ps1`): 162 backend tests pass, Alembic migrations clean, ESLint clean (0 errors), 180 frontend tests pass across 27 files, production build clean.
   - Product UAT (`run-product-uat.ps1`): 322/322 assertions pass, 0 failed, 0 blocking failed, 0 runtime errors.
   - Manifest reconciliation: `pass: true`.
6. **Visual Evidence Inspection**:
   - `pro07-ichimoku-cloud-1440x1000.png` (1440×1000, SHA-256: `79DAEC747D4A02816A90C180A3C147201FDD702818068FF080DA82290F57BFC3`): Verified. Price overlay chart renders Ichimoku Cloud (Tenkan, Kijun, Span A, Chikou) alongside BBands, SMA, and EMA cleanly with distinct colors, labels, and exact values.
   - `pro07-ichimoku-cloud-1280x800.png` (1280×800, SHA-256: `FA0B787C4A7319DA204034AFF79BEEA2FF1DA49BA916C65F11C033D7C5418795`): Verified. Compact 1280×800 viewport displays all overlays and subpanes responsively without visual clipping or overflow.
7. **Database Invariant**:
   - `backend/sumi.db` SHA-256 before/after: `F890F5BC16ECE557EA78E19A6095A362DE8641E341382DF66D6A9C997E84F080` (0 bytes mutated).
8. **Whitespace Check**:
   - `git diff --check`: 0 errors.

## Acceptance Criteria Summary

| ID | Status | Evidence |
| --- | --- | --- |
| PRO-IND-01 | PASS | Registry-driven calculation for Ichimoku Cloud; backend parity verified. |
| PRO-IND-02 | PASS | Exhaustive released catalog in `indicatorDomain.ts` and `IndicatorRenderRegistry.ts` with price overlay placement, styles, formats, and series. |
| PRO-IND-03 | PASS | Unreleased/engine-only definitions fail closed without fallback to EMA (verified in unit tests). |
| PRO-IND-04 | PASS | Semantic series keys/labels for Ichimoku (`tenkan`, `kijun`, `spanA`, `spanB`, `chikou`). |
| PRO-IND-05 | PASS | Focused overlay, multi-line, and cloud renderer contracts verified in unit tests and UAT. |
| PRO-IND-06 | PASS | Multi-instance lifecycle, addition, removal, reload, and navigation verified. |
| PRO-IND-07 | PASS | Warm-up nulls, gaps, and replay boundaries handled without invalid segments or future data leakage. |
| PRO-IND-08 | PASS | Parity tests cover standard and non-default parameter cases across replay API and strategy adapter. |
| PRO-IND-10 | PASS | Ichimoku displacement is a documented display transform and cannot reveal a value calculated from a future candle. |
| PRO-IND-11 | PASS | Browser UAT passes with 322/322 assertions and retained 1440×1000 and 1280×800 screenshots. |

## Disposition & Authorization Gate

PRO-07 is **CLOSED** with full independent approval. PRO-08 (Trade Planning and Journal) remains **UNAUTHORIZED** until explicitly authorized by the user.
